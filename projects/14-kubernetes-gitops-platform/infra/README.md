# Kubernetes GitOps Platform Infrastructure

This folder contains Terraform for the Kubernetes GitOps Platform project.

Keep EKS, networking, IAM, add-ons, variables, outputs, and backend examples here. Do not commit local state, secrets, or real `terraform.tfvars` files.
