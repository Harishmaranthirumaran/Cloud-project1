# Kubernetes GitOps Platform GitOps

This folder contains GitOps assets for the Kubernetes GitOps Platform project.

Keep Argo CD applications, Kubernetes manifests, Helm values, and Kustomize overlays here. Keep cluster infrastructure in `../infra`.
