# Kubernetes GitOps Platform Services

This folder contains service code for the Kubernetes GitOps Platform project.

Keep sample services, Dockerfiles, health checks, and service tests here. Keep deployment manifests in `../gitops`.
