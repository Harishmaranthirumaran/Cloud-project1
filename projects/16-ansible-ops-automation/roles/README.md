# Ansible Ops Automation Roles

This folder contains reusable Ansible roles for the Ansible Ops Automation project.

Keep role tasks, handlers, defaults, templates, and role tests here. Keep environment-specific values in `../group_vars`.
