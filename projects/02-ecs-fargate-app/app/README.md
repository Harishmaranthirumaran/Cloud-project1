# ECS Fargate App Application

This folder contains the containerized application for the ECS Fargate App project.

Keep runtime code, dependency manifests, Dockerfiles, and app tests here. Keep AWS infrastructure in `../infra` and deployment notes in `../docs`.
