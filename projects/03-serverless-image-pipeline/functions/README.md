# Serverless Image Pipeline Functions

This folder contains Lambda function code for the Serverless Image Pipeline project.

Keep handlers, shared helpers, and function-specific runtime files here. Keep tests in `../tests` and infrastructure in `../infra`.
