# Maven Cloud Service App

This folder contains the Java service for the Maven Cloud Service project.

Keep Maven source, tests, Dockerfiles, and build configuration here. Keep service documentation in `../docs`.
