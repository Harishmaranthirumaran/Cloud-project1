# Terraform Blueprint Registry Environments

This folder contains environment examples for the Terraform Blueprint Registry project.

Keep sample root modules and promotion structure here. Do not commit real backend files, secrets, state, or local `terraform.tfvars` files.
