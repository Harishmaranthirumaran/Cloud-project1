# Terraform Blueprint Registry Blueprints

This folder contains reusable Terraform blueprints for the Terraform Blueprint Registry project.

Keep opinionated module compositions, examples, and versioned blueprint patterns here. Keep environment wiring in `../environments`.
